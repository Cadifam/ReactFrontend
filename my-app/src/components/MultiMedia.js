import ReactPlayer from "react-player/youtube";

const MultiMedia = () => {return <ReactPlayer url="https:/www.youtube.com/watch?v=qfB3beKitig"
playing={true} volume={0.5} controls/>
};

export default MultiMedia;